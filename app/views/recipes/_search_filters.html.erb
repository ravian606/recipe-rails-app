<div class="row">
    <div class="form-group">
        <%= form_tag(recipes_path, method: :get) do %>
            <p> Search for a recipe by title </p>
            <%= text_field_tag(:search, params[:search]) %>
            <%= submit_tag ("Search") %>
    <% end %>
    </div>

     <div class="form-group ml-5">
        <%= form_tag(recipes_path, method: :get) do %>
            <p> Search for a recipe by difficulty </p>
            <%= text_field_tag(:difficulty, params[:difficulty]) %>
            <%= submit_tag ("Search") %>
        <% end %>
    </div>

    <div class="form-group ml-5">
        <%= form_tag(recipes_path, method: :get) do %>
            <p> Search for a recipe by time </p>
            <%= text_field_tag(:time, params[:time]) %>
            <%= submit_tag ("Search") %>
        <% end %>
    </div>
</div>